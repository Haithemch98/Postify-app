const { User, Admin, SimpleUser } = require('./user_profile');
const Post = require('./post');
const Comment = require('./comment');

module.exports = {
    User,
    Admin,
    SimpleUser,
    Post,
    Comment
};
